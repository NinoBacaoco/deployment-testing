spring.application.name=Capstone-1

# Database Connection Configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${RENDER_DATABASE_URL}:${RENDER_DATABASE_PORT}/${RENDER_DATABASE_NAME}
spring.datasource.username=${RENDER_DATABASE_USER}
spring.datasource.password=${RENDER_DATABASE_PASSWORD}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Static
spring.mvc.static-path-pattern=/**

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mieminabua@gmail.com
spring.mail.password=rylqehmorjabrchm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Path of uploaded files
file.path=/app/capstone1/  # Adjusted for container filesystem
certificate.path=/app/capstone1/certificates/  # Adjusted for container filesystem

# To set the file size of the uploaded file
spring.servlet.multipart.max-file-size=128MB
spring.servlet.multipart.max-request-size=128MB
spring.servlet.multipart.enabled=true

# Cipher
cipher.algorithm=AES
cipher.secret_key=mySecretKey12345
